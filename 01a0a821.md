# 短提示词映射（本会话）

用户短句（任意一条）：

```text
安装 https://github.com/shaohuawen03-cyber/new/tree/arena/01a0aeb9-new 的 skills，与本机打通
安装 git-sync skills，与本地打通
```

**不要打开 arena.ai**（登录墙，沙箱打不开）。技能只在 GitHub。

## 本会话的真值

| 项 | 值 |
|---|---|
| 技能来源 | `https://github.com/shaohuawen03-cyber/new.git` 分支 `arena/01a0aeb9-new`（commit `dadc1c8`） |
| 技能版本 | `skills/git-sync/VERSION` = **2.9.2**（与技能总部分支 `arena/01a0ae7a-new` 同版本，两者 `skills/git-sync/` 仅 `sync.config.json` 不同） |
| 本会话仓库 | `https://github.com/shaohuawen03-cyber/BrowserSkill.git` |
| 本会话分支 | `arena/01a0b23a-browserskill` |
| 本机克隆目录 | `E:\0github\git-sync\BrowserSkill-01a0b23a`（新目录，不覆盖已有克隆） |
| 钉住的账号 | `shaohuawen03-cyber`（谁仓库的会话就用谁账号） |

## 本机命令块

**不要手打**，由脚本生成（真值已替换、分支与 HEAD 不一致会 exit 3 拒绝）：

```bash
bash skills/git-sync/scripts/agent-handoff.sh
```

「与本地打通」= 用户在 Windows 上跑完那段（值守注册好、心跳在走）。在那之前不算打通。

本仓库特有的两处适配见 `code/git-sync-local-notes.md`。
